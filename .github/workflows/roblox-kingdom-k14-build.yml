name: Build AstraLife Kingdom K14 Sovereignty

on:
  push:
    branches: [feat/roblox-kingdom-k14-sovereignty]
    paths: ["roblox/**", "rokit.toml", ".github/workflows/roblox-kingdom-k14-build.yml"]
  pull_request:
    branches: [feat/roblox-kingdom-k0-core]
    paths: ["roblox/**", "rokit.toml", ".github/workflows/roblox-kingdom-k14-build.yml"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with: { version: "v1.1.0" }
      - run: rojo build roblox/default.project.json -o AstraLife-K14.rbxl
      - uses: actions/upload-artifact@v4
        with:
          name: AstraLife-K14-Sovereignty
          path: AstraLife-K14.rbxl
          if-no-files-found: error
          retention-days: 14
