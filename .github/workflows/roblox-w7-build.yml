name: Build AstraLife Roblox W7

on:
  push:
    branches:
      - feat/roblox-w7-ecosystem
    paths:
      - "roblox/**"
      - "rokit.toml"
      - ".github/workflows/roblox-w7-build.yml"
  pull_request:
    branches:
      - feat/roblox-w6-survival-bridge
    paths:
      - "roblox/**"
      - "rokit.toml"
      - ".github/workflows/roblox-w7-build.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rokit
        uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: "v1.1.0"

      - name: Build Roblox place
        run: rojo build roblox/default.project.json -o AstraLife-W7.rbxl

      - name: Upload W7 place
        uses: actions/upload-artifact@v4
        with:
          name: AstraLife-Roblox-W7
          path: AstraLife-W7.rbxl
          if-no-files-found: error
          retention-days: 14
