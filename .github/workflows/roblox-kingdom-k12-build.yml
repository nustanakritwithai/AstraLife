name: Build AstraLife Kingdom K12 Merchant Guild

on:
  push:
    branches: [feat/roblox-kingdom-k12-merchant-guild]
    paths: ["roblox/**", "rokit.toml", ".github/workflows/roblox-kingdom-k12-build.yml"]
  pull_request:
    branches: [feat/roblox-kingdom-k0-core]
    paths: ["roblox/**", "rokit.toml", ".github/workflows/roblox-kingdom-k12-build.yml"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with: { version: "v1.1.0" }
      - run: rojo build roblox/default.project.json -o AstraLife-K12.rbxl
      - uses: actions/upload-artifact@v4
        with:
          name: AstraLife-K12-MerchantGuild
          path: AstraLife-K12.rbxl
          if-no-files-found: error
          retention-days: 14
